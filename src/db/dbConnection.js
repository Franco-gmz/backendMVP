/*var mysql = require('mysql');

var connection = mysql.createConnection({
  host     : '127.0.0.1',
  user     : 'root',
  password : 'password',
  database : 'psa',
  port: '3306'
});
 
connection.connect();

module.exports = connection;*/
